name: Temporary image-to-PDF benchmark — macOS (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: macos-15
    steps:
      - run: echo disabled
