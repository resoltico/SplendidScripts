name: Temporary optimized image-to-PDF rerun (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo superseded
