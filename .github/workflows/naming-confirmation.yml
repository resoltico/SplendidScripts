name: Atomic naming guidance comparison

on:
  push:
    branches: [experiment/naming-guidance-evaluation]
    paths:
      - .github/naming-confirmation/**
      - .github/workflows/naming-confirmation.yml

permissions:
  contents: read

jobs:
  evaluate:
    name: ${{ matrix.model }} / ${{ matrix.condition }}
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        model: [qwen-small, qwen-medium]
        condition: [K7, M2, R9, T4]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Run atomic naming comparison
        run: python3 .github/naming-confirmation/confirm.py --worker "$MODEL" "$CONDITION"
        env:
          MODEL: ${{ matrix.model }}
          CONDITION: ${{ matrix.condition }}
          PYTHONUNBUFFERED: '1'
      - name: Preserve raw responses and scores
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: atomic-${{ matrix.model }}-${{ matrix.condition }}
          path: confirmation-results
          retention-days: 14
          if-no-files-found: error

  aggregate:
    name: Apply frozen selection rule
    needs: evaluate
    if: always()
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v4
        with:
          pattern: atomic-*
          path: evidence
          merge-multiple: true
      - name: Aggregate naming results
        run: |
          cp .github/naming-evaluation/evaluate.py evidence/evaluate.py
          cp .github/naming-confirmation/confirm.py evidence/confirm.py
          cp .github/workflows/naming-confirmation.yml evidence/workflow.yml
          python3 .github/naming-confirmation/confirm.py --aggregate evidence
      - name: Preserve complete evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: atomic-naming-evidence
          path: evidence
          retention-days: 14
          if-no-files-found: error
