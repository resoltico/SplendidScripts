name: Temporary image-to-PDF visual QA — macOS (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: macos-15
    steps:
      - run: echo completed
