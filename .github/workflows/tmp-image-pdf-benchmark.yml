name: Temporary image-to-PDF benchmark — Linux (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "This temporary Linux workflow is disabled after its benchmark run."
